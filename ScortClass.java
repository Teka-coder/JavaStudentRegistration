/*
package studentregistration;


public class ScortClass {
    
}
*/

/*
import java.io.FileReader;
import java.io.FileWriter;
import java.io.BufferedReader;
import java.io.BufferedWriter;
public void show() throws FileNotFoundException{
        String path;
        path = "C:\\Users\\hi\\Documents\\NetBeansProjects\\studentRegistration\\teka.txt";
        Scanner ps=new Scanner(new FileReader(path));
        while(ps.hasNext()){
            String per=ps.nextLine();
            System.out.println(per);
        }
        ps.close();

    } */
/*
//  BufferedWriter writer= new BufferedWriter(new FileWriter("myfilestudent.txt",true));
      // writer.equals(ss);//it takes an object
       // writer.write(Registering.t);
      //writer.toString();
       // writer.close();
        
       //writer.

//  BufferedWriter datawriter= new BufferedWriter(new FileWriter("myfileregistered.txt",true));
       // datawriter.write(Registering.t);
       // datawriter.close();
//DataOutputStream dos = new DataOutputStream(new FileOutputStream("grade.txt"));
        //dos.writeFloat(gt.cgpa);  //recored the gpa by float
        //dos.close();

//BufferedReader reader= new BufferedReader(new FileReader("myfileregistered.txt"));
       // boolean b1=reader.ready();//like exist();
*/
// BufferedReader datareader= new BufferedReader(new FileReader("myfileregistered.txt"));
            //if file exist read from file by corresponding data type
           //int t1=datareader.read();//read int
            
           // datareader.close();
            
             //give the int observed by read();
            //Registering.t = t1;  //yetenebebewun id le registering yasayal/ysexal
            
            /*  file=new java.io.File("grade.txt");
        boolean bo = file.exists();
        if(bo) {
            if(t1==0);
            else {
               DataInputStream dis = new DataInputStream(new FileInputStream("grade.txt"));
        //if file of registered find it do/read from also for grade file
        float gp=dis.readFloat();
        dis.close();
        Gradetest.cgpa=gp; 
            }
        } else{
            Registering.t = 0;  //else no one registered
        }  */
       
       // reader= new BufferedReader(new FileReader("myfilestudent.txt"));//to read student file
// boolean ba=reader.ready();// checking if exist
//ke 0 yeteleye kehone gn kexlo yseral/ read from file yaregal....mnun? student filen
                  //slezi le registered ytayal malet nw...yeteseraw gn le profile database neber
      /*   BufferedReader reading= new BufferedReader(new FileReader("myfilestudent.txt"));
         ss=(Student[])
         reading.read();
          reading.close();
         //reading.equals(); equals() is boolean i think it is to compare    */